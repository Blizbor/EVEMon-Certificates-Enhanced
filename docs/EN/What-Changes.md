# What changes

The goal is stronger contrast between “just unlocked”, “T2 available”, “competent”, “strong” and “specialist”. Shared support skills are separated from weapon access where that makes the certificate easier to read. The project patches certificates only; it does not patch `eve-skills` or `eve-masteries`.
